import 'package:flutter/material.dart';

class ScatterplotPage extends StatelessWidget {
  const ScatterplotPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
